import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData lightTheme() {
    return ThemeData(
        // Define your app's theme here
        );
  }

  static ThemeData darkTheme() {
    return ThemeData.dark().copyWith(
        // Define your app's dark theme here
        );
  }
}
